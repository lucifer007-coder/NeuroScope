fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
numpy==1.26.4
# CPU-only PyTorch — install separately on deployment with:
#   pip install torch==2.3.1+cpu --extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1
python-multipart==0.0.9